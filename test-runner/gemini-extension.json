{
  "name": "test-runner",
  "version": "1.0.0",
  "description": "Run Go tests and summarize failures with rerun arguments.",
  "mcpServers": {
    "test-runner": {
      "command": "${extensionPath}${/}bin${/}test-runner"
    }
  }
}
