# Antigravity CLI (agy) プラグイン開発の既知の問題と回避策まとめ

現在の `agy` において、プラグイン開発・運用時に踏み抜きやすい既知のバグや仕様の罠、およびその回避策をまとめました。（Issue #390, #395, #551 等の現行バージョンの検証に基づく）

## 1. パス変数が展開されないバグ (Issue #390, #395)
ネイティブフォーマット（`plugin.json` 形式）を使用した場合、`mcp_config.json` や `hooks.json` の設定値に `${extensionPath}` および `${/}` を記述しても、インストール時にパスが絶対パスに解決されずリテラル文字列のまま配置されてしまいます。結果としてバイナリ起動などに失敗します。

> [!TIP]
> **回避策**
> - **`mcp_config.json`**: 同梱バイナリなどを絶対パスで参照したい場合は、ネイティブフォーマットを使用せず、レガシーな `gemini-extension.json` 形式で作成します（この形式であればインストール時にパス展開が走ります）。
> - **`hooks.json`**: 変数を使わず、カレントディレクトリからの相対パス（例: `"command": "bin/wrapper"`）で記述します。フック実行時のカレントディレクトリ (PWD) がプラグインのインストール先ディレクトリになる仕様を逆手にとります。

## 2. プラグイン定義のサブエージェントが弾かれる (Issue #551)
プラグイン内に独自のサブエージェント（`plugins/<name>/agents/<agent_name>/agent.json`）を同梱した場合、インストール時のバリデーション（`agy plugin validate`）は通りますが、会話エンジンからの呼び出し時に `subagent "..." not found or not allowed to be invoked` となり実行がブロックされます。

> [!IMPORTANT]
> **回避策**
> 現状はプラグインスコープでの専用サブエージェント定義を避け、プロンプトやタスクの専門知識は `skills/<name>/SKILL.md`（スキル）として提供する構成に倒すのが確実です。

## 3. プラグインからの `rules/` 自動注入が無視される
プラグイン内に `rules/*.md` を配置してエージェントのシステムプロンプトに独自の規約を暗黙的に注入しようとしても、実環境ではエージェントに一切反映されません。

> [!NOTE]
> **回避策**
> エージェントに対してプラグイン固有の知識・規約を渡したい場合は、必ず呼び出し可能な `skills/<name>/SKILL.md` として定義します。

## 4. MCPサーバーの stdout 出力による通信破壊
同梱したバイナリや起動用のラッパースクリプトが少しでも標準出力（`stdout`）にデバッグログなどを吐いてしまうと、MCPが使用する NDJSON プロトコルが破壊され、サーバーが即クラッシュします。

> [!WARNING]
> **回避策**
> ラッパースクリプトやバイナリの処理経過・エラー等の診断ログは、必ずすべて標準エラー出力（`stderr`）に向けるように実装してください。

## 5. StatefulなMCPサーバーのデータワイプ (Issue #554)
認証の OAuth トークンなどを保持する Stateful な MCP サーバーの場合、プラグインのアップグレード（`agy plugin install` の再実行）を行うと、プラグインディレクトリ内の保存データごとワイプされてしまいます。

> [!CAUTION]
> **回避策**
> 認証情報やユーザーの永続化データは、プラグインのインストールディレクトリ（`~/.gemini/config/plugins/<name>`）に置かず、ホームディレクトリ配下などに独立した保存領域を設けて管理する設計にしてください。
