{
  "name": "github",
  "version": "0.2.0",
  "description": "Official GitHub MCP server (github/github-mcp-server)",
  "mcpServers": {
    "github": {
      "command": "${extensionPath}${/}mcpServers${/}github-mcp-wrapper.sh"
    }
  }
}
